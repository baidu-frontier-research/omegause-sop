1. 当运行到用户输入有指定要设置某些参数，如设置采光面倾角30°，以用户为主而不是操作提示中的值或者历史输入的值，如果是click操作则可以忽略这一条。
2. 在执行下一步操作前请一定要检查上一步的操作是否生效，如果点击没生效，可能因为app反应较慢，请先稍微等待一下，如果还是不生效请调整一下点击位置重试，如果仍然不生效请跳过这一步继续任务，并在finish message说明点击不生效。
3. 如果要修改 系统-输入计划功率 的内容，点击文本框进行修改
4. 修改 系统-串联组件数 时，如果要减少，就点击向下箭头按钮；如果要增加，就click向上箭头按钮
